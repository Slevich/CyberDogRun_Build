{
  "name": "cyber-dog-run",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site — no build needed'",
    "dev": "echo 'No dev server'",
    "start": "echo 'Standalone'"
  }
}